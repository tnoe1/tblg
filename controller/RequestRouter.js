const LoggedEntity = require("../lib/LoggedEntity");

class RequestRouter {
    constructor() {
        super("req-router");
    }

    // TODO: Might need access to services here
    route(req_obj) {}
}

module.exports = RequestRouter;

